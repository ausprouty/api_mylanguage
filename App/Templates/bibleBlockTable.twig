<table class="{{ dir_language1 }} dbs">
    {{ passage_rows | raw }}
    <tr>
        <td class="{{ dir_language1 }} dbs link">
            <a href="{{ url }}">{{ read_more }}</a>&nbsp;&nbsp;&nbsp;
            <img src="{{ qr_code1 }}" width="100px">
        </td>
        <td class="{{ dir_language2 }} dbs link">
            <a href="{{ url }}">{{ read_more }}</a>&nbsp;&nbsp;
            <img src="{{ qr_code2 }}" width="100px">
        </td>
    </tr>
</table>
