<div class="{{ dir_language }} dbs">
    <span class="biblereference">{{ bible_reference }}</span>
    <br>
    {{ passage_text }}
    <br>
    <span class="{{ dir_language }} dbs link">
        <a href="{{ url }}">{{ read_more }}</a>
    </span>
</div>
